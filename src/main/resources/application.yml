server:
  port: 8080

spring:
  application:
    name: my-board
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/myboard
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true