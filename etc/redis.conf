appendonly yes             # AOF 활성화
appendfsync everysec       # 1초 주기로 디스크 flush
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size  64mb  # AOF가 64 MB 이상 + 100 % 성장 시 Rewrite
save ""                    # RDB 불필요 시 끔(또는 주기 설정)
